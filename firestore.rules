rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /events/tuesday {
      allow read: if request.auth != null;
      allow write: if false; // server-side only via Admin SDK
    }
  }
}
